version: '2.1'
orbs:
  sam: circleci/aws-sam-serverless@3.0
jobs:
  build_lambda:
    executor: sam/default
    steps:
      - checkout
      - sam/install
      - run: gradle build -x test
workflows:
  kotlin-pipeline:
    jobs:
      - build_lambda