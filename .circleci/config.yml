version: '2.1'
orbs:
  sam: circleci/aws-sam-serverless@3.0
jobs:
  build_app:
    executor: sam/default
    context: aws-credentials-context
    steps:
      - checkout      
      - sam/install
      - run: sam build
workflows:
  my-workflow:
    jobs:
      - build_app